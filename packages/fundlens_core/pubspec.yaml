name: fundlens_core
description: "FundLens 纯 Dart 领域层:资产分类、估值、盈亏、覆盖率、集中度与快照差异计算。"
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  collection: ^1.19.1
  decimal: ^3.2.5
  meta: ^1.18.0
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
