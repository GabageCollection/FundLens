# FundLens v1.0.0 发布测试报告

> 本报告只记录**实际观察到**的证据。无法在当前开发机执行的项目标注
> “待发布机器执行”。模板见 `docs/test-report-template.md`。

## 基本信息

| 项目 | 值 |
|---|---|
| 版本 | 1.0.0（发布候选） |
| 报告日期 | 2026-07-25 |
| 执行机器 | 开发机，Windows 11 专业版 10.0.22631（build 22631） |
| 安装包路径 | 待发布机器执行（本机未安装 Inno Setup，未产出安装包） |
| 安装包 SHA-256 | 待发布机器执行 |
| 依赖锁定哈希 | 待发布机器执行（由 `tests/release/clean_vm_acceptance.ps1` 自动记录 `apps/fundlens_windows/pubspec.lock`、`packages/fundlens_core/pubspec.lock`、`engine/requirements.lock` 的 SHA-256） |

## 自动化测试证据

| 命令 | 结果（实际数字） | 通过? |
|---|---|---|
| `dart test packages/fundlens_core` | 18 个测试全部通过 | 是 |
| `flutter test apps/fundlens_windows`（经 `tools/with_sqlite3mc_server.py` 包装） | 201 个测试全部通过 | 是 |
| `flutter analyze apps/fundlens_windows` | No issues found | 是 |
| `python -m pytest engine/tests -m "not live" -q` | 58 passed, 1 deselected | 是（见下方说明） |
| `python -m ruff check engine` | All checks passed | 是 |
| `python -m mypy engine/src` | Success: no issues found in 20 source files | 是 |
| `flutter test apps/fundlens_windows/integration_test/<file>`（逐文件，经包装器） | 3 个文件各 1 个测试全部通过（performance、encrypted_database、windows_workflow） | 是 |
| `powershell -File tools/build_windows_release.ps1` | 待发布机器执行 | — |
| `powershell -File tests/release/clean_vm_acceptance.ps1 <安装包>` | 待发布机器执行（脚本已完成并通过 `[ScriptBlock]::Create` 语法解析检查，本机无干净 VM 与安装包，未执行） | — |

pytest 说明：本机默认临时目录 `C:\Users\84254\AppData\Local\Temp\pytest-of-84254`
的访问控制列表损坏（`os.scandir` 返回 WinError 5），导致 `tests/test_security.py`
中 10 个使用 `tmp_path` 的测试在 **fixture setup 阶段**报错（非断言失败）。改用
`--basetemp=./.pytest_tmp` 后 58 个测试全部通过。发布机器上应直接以默认命令复跑
确认退出码为 0。

integration_test 说明：整目录一次运行（`flutter test integration_test`）在本机
无法启动设备端应用（log reader 失败）；逐文件运行全部通过。发布门禁按逐文件方式
执行。

## 性能实测（2,000 持仓 / 500 快照）

测试：`apps/fundlens_windows/integration_test/performance_test.dart`，
开发机 Debug 模式 widget 测试（1440×900），测量时本机另有大型构建任务占用 CPU。

| 指标 | 预算 | 实测 | 通过? |
|---|---|---|---|
| 首次渲染并稳定 | < 3000 ms | 1559 ms | 是 |
| 切换到“资产分析”并稳定 | < 500 ms | 450 ms | 是（余量较小，见下） |
| 持仓仓库全量读取次数 | = 1 | 1 | 是 |
| 数据引擎启动次数 | = 1 | 1 | 是 |

“资产分析”切换实测 450 ms，接近 500 ms 预算。考虑到这是 Debug 模式且测量期间
存在 CPU 争用，未放宽预算、未修改生产代码。发布机器需在 profile/release 模式下
复跑确认余量。

## 干净 VM 验收清单（8 项，全部使用合成数据）

待发布机器执行。执行脚本：`tests/release/clean_vm_acceptance.ps1`
（交互式引导 8 项清单并自动记录证据 JSON），清单内容：

1. [ ] 手动添加现金、存款和实物黄金 — 待发布机器执行
2. [ ] 支付宝截图部分导入并修正一处低置信度字段 — 待发布机器执行
3. [ ] 同花顺截图导入，核对名称/金额/份额/成本/正负号 — 待发布机器执行
4. [ ] 行情刷新；支付宝纯金额持仓金额不变，失败保留上次有效值 — 待发布机器执行
5. [ ] 保存两个快照并对比“资产金额变化” — 待发布机器执行
6. [ ] 加密备份；错误密码被拒绝；正确密码恢复成功 — 待发布机器执行
7. [ ] 杀掉数据引擎后进入降级模式，手动/缓存数据可用 — 待发布机器执行
8. [ ] 全应用无投资建议措辞；安装目录无日志/备份/用户文件 — 待发布机器执行

第 8 项的安装目录扫描已由脚本自动完成，无需手工核对。

## 升级与卸载

| 项目 | 结果 |
|---|---|
| 旧版本覆盖安装后数据库保留 | 待发布机器执行（需提供上一构建安装包 `-PreviousInstallerPath`） |
| 覆盖安装后持仓与快照可见 | 待发布机器执行 |
| 静默卸载后 `%APPDATA%\FundLens` 保留 | 待发布机器执行（脚本自动校验；静默卸载无法勾选删除项，数据必须保留） |

## 已由自动化测试覆盖的发布门禁项（本机证据）

- 备份加密通过篡改、错误密码和随机性（每份备份独立盐/nonce）测试 — `flutter test` 201 通过中含 `test/backup/backup_cipher_test.dart`；
- 恢复失败不改变当前数据；替换失败后恢复可恢复副本 — 同中含 `test/backup/database_restore_service_test.dart`；
- 日志与引擎诊断脱敏 — 同中含 `test/security/redacting_logger_test.dart`；
- 已安装应用只启动自带的版本匹配引擎 — 同中含 `test/data_engine/installed_engine_locator_test.dart`。

## 未解决的发布阻断缺陷

- 本机 pytest 默认临时目录 ACL 损坏导致 10 个测试 setup 报错（环境问题，绕过后全绿）；发布机器复跑确认。
- 干净 VM 安装、升级、恢复、降级、卸载保留未执行：待发布机器执行。
- 安装包未构建、SHA-256 未记录：待发布机器执行。

## 结论

开发机可执行的自动化门禁全部通过；性能预算实测达标。干净 VM 验收、安装包
构建与哈希记录尚未执行，**发布门禁暂不通过**，须在发布机器完成上述
“待发布机器执行”项后更新本报告。
